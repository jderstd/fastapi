[project]
name = "workspace"
version = "0.0.0"
classifiers = ["Private :: Do Not Upload"]

[tool.uv.workspace]
members = [
    "package",
    "test",
    "example",
]

[tool.uv.sources]
jder-fastapi = { workspace = true }

[dependency-groups]
dev = [
    "ruff>=0.14.10,<0.15.0",
    "ty>=0.0.5,<0.1.0",
]
